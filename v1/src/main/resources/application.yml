spring:
  application:
    name: v1
  datasource:
    url: jdbc:postgresql://localhost:5455/mapGenerator_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
